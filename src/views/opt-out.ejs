<%- include('layout', { body: ` 
<section class="py-16">
    <div class="max-w-md mx-auto px-4 text-center">
        ${typeof error !== 'undefined' && error ? `
        <div class="text-6xl mb-6">❌</div>
        <h1 class="text-2xl font-bold mb-4 text-red-600">Oops!</h1>
        <p class="text-gray-600 mb-8">${error}</p>
        <a href="/" class="inline-block bg-bitcoin text-white px-6 py-3 rounded-lg font-bold hover:bg-bitcoin-dark transition">
            Go Home
        </a>
        ` : `
        <div class="text-6xl mb-6">👋</div>
        <h1 class="text-2xl font-bold mb-4">You've Been Removed</h1>
        <p class="text-gray-600 mb-8">${typeof message !== 'undefined' ? message : 'You have been removed from the Bitcoin Review Raffle.'}</p>
        <p class="text-gray-500 text-sm mb-8">
            Changed your mind? You can always register again!
        </p>
        <a href="/#register" class="inline-block bg-bitcoin text-white px-6 py-3 rounded-lg font-bold hover:bg-bitcoin-dark transition">
            Register Again
        </a>
        `}
    </div>
</section>
` }) %>
