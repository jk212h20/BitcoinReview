<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/styles.css" rel="stylesheet">
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Navigation -->
    <nav class="bg-white shadow-sm">
        <div class="max-w-6xl mx-auto px-4 py-4">
            <div class="flex justify-between items-center">
                <a href="/" class="flex items-center space-x-2">
                    <span class="text-2xl">₿</span>
                    <span class="font-bold text-xl">Bitcoin Review Raffle</span>
                </a>
                <div class="hidden md:flex items-center space-x-6">
                    <a href="/" class="text-gray-600 hover:text-bitcoin">Home</a>
                    <a href="/submit" class="text-gray-600 hover:text-bitcoin">Submit Review</a>
                    <a href="/reviews" class="text-gray-600 hover:text-bitcoin">Reviews</a>
                    <a href="/raffles" class="text-gray-600 hover:text-bitcoin">Raffles</a>
                    <a href="/merchants" class="text-gray-600 hover:text-bitcoin">Merchants</a>
                    <a href="/how-it-works" class="text-gray-600 hover:text-bitcoin">How It Works</a>
                    <a href="/#donate" id="raffle-fund-badge" class="hidden items-center gap-1.5 bg-gradient-to-r from-orange-500 to-yellow-500 text-white px-3 py-1.5 rounded-full text-sm font-bold shadow-sm hover:shadow-md transition-all" title="Next raffle prize">
                        <span>⚡</span>
                        <span>Next Prize:</span>
                        <span id="raffle-fund-amount">0</span>
                        <span class="text-xs opacity-80">sats</span>
                    </a>
                </div>
                <div class="md:hidden">
                    <button id="mobile-menu-btn" class="text-gray-600">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                        </svg>
                    </button>
                </div>
            </div>
            <!-- Mobile menu -->
            <div id="mobile-menu" class="hidden md:hidden mt-4 space-y-2">
                <a href="/" class="block py-2 text-gray-600 hover:text-bitcoin">Home</a>
                <a href="/submit" class="block py-2 text-gray-600 hover:text-bitcoin">Submit Review</a>
                <a href="/reviews" class="block py-2 text-gray-600 hover:text-bitcoin">Reviews</a>
                <a href="/raffles" class="block py-2 text-gray-600 hover:text-bitcoin">Raffles</a>
                <a href="/merchants" class="block py-2 text-gray-600 hover:text-bitcoin">Merchants</a>
                <a href="/how-it-works" class="block py-2 text-gray-600 hover:text-bitcoin">How It Works</a>
                <a href="/#donate" id="raffle-fund-badge-mobile" class="hidden py-2 text-bitcoin font-bold">
                    ⚡ Next Prize: <span id="raffle-fund-amount-mobile">0</span> sats
                </a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        <%- body %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-16">
        <div class="max-w-6xl mx-auto px-4 py-8">
            <div class="grid md:grid-cols-3 gap-8">
                <div>
                    <h3 class="font-bold text-lg mb-4">Bitcoin Review Raffle</h3>
                    <p class="text-gray-400">Incentivizing Bitcoin adoption in Roatan through community reviews.</p>
                </div>
                <div>
                    <h3 class="font-bold text-lg mb-4">Quick Links</h3>
                    <ul class="space-y-2 text-gray-400">
                        <li><a href="/submit" class="hover:text-white">Submit a Review</a></li>
                        <li><a href="/merchants" class="hover:text-white">Find Merchants</a></li>
                        <li><a href="/how-it-works" class="hover:text-white">How It Works</a></li>
                    </ul>
                </div>
                <div>
                    <h3 class="font-bold text-lg mb-4">Support the Raffle</h3>
                    <p class="text-gray-400 text-sm mb-3">Every sat donated goes directly into the prize pool.</p>
                    <a href="/#donate" class="inline-flex items-center gap-2 bg-bitcoin hover:bg-bitcoin-dark text-white px-4 py-2 rounded-lg text-sm font-medium transition">
                        ⚡ Donate (On-chain or Lightning)
                    </a>
                </div>
            </div>
            <div class="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400">
                <p>🌴 Made with ⚡ in Roatan</p>
            </div>
        </div>
    </footer>

    <script>
        // Mobile menu toggle
        document.getElementById('mobile-menu-btn')?.addEventListener('click', function() {
            document.getElementById('mobile-menu').classList.toggle('hidden');
        });

        // ── Live Raffle Fund Display ──────────────────────────────────
        (function() {
            var fundBadge = document.getElementById('raffle-fund-badge');
            var fundAmount = document.getElementById('raffle-fund-amount');
            var fundBadgeMobile = document.getElementById('raffle-fund-badge-mobile');
            var fundAmountMobile = document.getElementById('raffle-fund-amount-mobile');
            var homepageFund = document.getElementById('homepage-fund-amount');
            var lastAmount = -1;

            function formatSats(n) {
                return Number(n).toLocaleString();
            }

            function updateFundDisplay(sats) {
                if (sats === lastAmount) return;
                lastAmount = sats;

                // Desktop nav badge
                if (fundBadge && fundAmount) {
                    fundAmount.textContent = formatSats(sats);
                    if (sats > 0) {
                        fundBadge.classList.remove('hidden');
                        fundBadge.classList.add('inline-flex');
                    }
                }

                // Mobile nav badge
                if (fundBadgeMobile && fundAmountMobile) {
                    fundAmountMobile.textContent = formatSats(sats);
                    if (sats > 0) {
                        fundBadgeMobile.classList.remove('hidden');
                        fundBadgeMobile.classList.add('block');
                    }
                }

                // Homepage donation section (if present)
                if (homepageFund) {
                    homepageFund.textContent = formatSats(sats);
                    var wrapper = document.getElementById('homepage-fund-wrapper');
                    if (wrapper && sats > 0) {
                        wrapper.classList.remove('hidden');
                    }
                }
            }

            function fetchFund() {
                fetch('/api/raffle-fund')
                    .then(function(res) { return res.json(); })
                    .then(function(data) {
                        if (data.success && typeof data.nextPrizeSats === 'number') {
                            updateFundDisplay(data.nextPrizeSats);
                            // Also update the full fund display on homepage if present
                            var totalEl = document.getElementById('homepage-total-fund');
                            if (totalEl && typeof data.totalFundSats === 'number') {
                                totalEl.textContent = formatSats(data.totalFundSats);
                            }
                        }
                    })
                    .catch(function() { /* silent fail */ });
            }

            // Initial fetch
            fetchFund();

            // Poll every 30 seconds
            setInterval(fetchFund, 30000);
        })();
    </script>
</body>
</html>
